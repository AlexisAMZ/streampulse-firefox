{
  "name": "streampulse-firefox",
  "version": "26.9.22",
  "private": true,
  "description": "Outillage de developpement de l'extension StreamPulse pour Firefox (AMO). Rien ici n'est package dans le zip livre.",
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "verify": "node scripts/verify.mjs",
    "store:assets": "node scripts/make-store-assets.mjs",
    "build": "npm run lint && npm test && node scripts/build-zip.mjs",
    "test": "node --test \"tests/*.test.mjs\""
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@vitalets/google-translate-api": "^9.2.1",
    "eslint": "^10.8.0",
    "globals": "^17.9.0"
  }
}
